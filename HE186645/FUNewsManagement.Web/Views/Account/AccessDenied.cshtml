@{
    ViewData["Title"] = "Access Denied";
}

<style>
    body {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        color: #f8f9fa;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .access-denied-container {
        max-width: 500px;
        width: 100%;
        text-align: center;
        padding: 40px 20px;
    }

    .denied-card {
        border: none;
        border-radius: 1.5rem;
        overflow: hidden;
        box-shadow: 0 20px 60px rgba(0,0,0,0.35);
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        color: #2c3e50;
    }

    .card-body {
        padding: 3rem 2.5rem;
    }

    .icon-ban {
        font-size: 6rem;
        color: #dc3545;
        margin-bottom: 1.5rem;
        animation: pulse 2s infinite;
    }


    h2 {
        font-weight: 800;
        color: #dc3545;
        margin-bottom: 1rem;
    }

    .lead {
        font-size: 1.25rem;
        color: #495057;
        margin-bottom: 2rem;
    }

    .btn-home {
        background: linear-gradient(90deg, #667eea, #764ba2);
        border: none;
        color: white;
        padding: 0.75rem 2rem;
        font-size: 1.1rem;
        font-weight: 600;
        border-radius: 50px;
        transition: all 0.3s;
    }

        .btn-home:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 30px rgba(102, 126, 234, 0.5);
        }

    .page-title {
        color: white;
        font-weight: 700;
        margin-bottom: 2rem;
        text-shadow: 0 2px 10px rgba(0,0,0,0.4);
    }
</style>

<div class="access-denied-container">
    <h2 class="page-title display-5 fw-bold mb-5">
        <i class="fas fa-shield-alt me-3"></i>Access Denied
    </h2>

    <div class="denied-card">
        <div class="card-body">
            <i class="fas fa-ban icon-ban"></i>
            <h2>Truy cập bị từ chối</h2>
            <p class="lead mb-4">
                Bạn không có quyền truy cập vào trang này.<br />
                Có thể bạn cần đăng nhập với tài khoản phù hợp hoặc liên hệ quản trị viên.
            </p>

            <a asp-controller="Home" asp-action="Index" class="btn btn-lg btn-home">
                <i class="fas fa-home me-2"></i>Quay về Trang chủ
            </a>
        </div>
    </div>

    <p class="text-white mt-4 small">
        Nếu bạn nghĩ đây là lỗi, vui lòng <a href="#" class="text-white fw-bold">liên hệ hỗ trợ</a>.
    </p>
</div>